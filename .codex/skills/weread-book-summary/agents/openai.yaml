interface:
  display_name: "WeRead Book Summary"
  short_description: "Read WeRead books and write indexed KB notes"
  default_prompt: "Use $weread-book-summary to read this WeRead book link with my Chrome login, summarize it as a practical Chinese method note, write it into KnowledgeBase when requested, and keep the KB reading-note index updated."
